{{- if .Values.api.service.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sensors.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type | default "ClusterIP" }}
  selector:
    {{- include "sensors.api.selectorLabels" . | nindent 6 }}
  ports:
  {{- range $name, $port := .Values.api.service.port }}
  {{- if $port.enabled }}
    - name: {{ $name }}
      port: {{ $port.port }}
  {{- end }}
  {{- end }}
{{- end }}
